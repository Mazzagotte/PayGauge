# PayGauge Branding

Approved name: **PayGauge**

Tagline: **See what's left.**

The only approved logo source for the initial build is the exact mockup artwork stored under:

`apps/paygauge-web/public/brand/`

Do not redraw, reinterpret, or replace the logo during initial implementation.
